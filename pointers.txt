The memory addresses of the variables in this program change because the B is cast to the address of A plus an integer which is 4 bytes. This places the address off by 4 places compared to A. The memory address of C is is cast to A plus a character which is one byte. This causes its addess to shift by on byte compared to A.
